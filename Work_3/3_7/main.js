const numbers = prompt("")
const sdvig = +prompt("")

const srez = numbers.slice(0, sdvig)
const srez2 = numbers.slice(sdvig, sdvig.lenght)
console.log('srez1 =', srez)
console.log('srez2 =', srez2)

console.log('srez2 + srez', srez2 + srez)
